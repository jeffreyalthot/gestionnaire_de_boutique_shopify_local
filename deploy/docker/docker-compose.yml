services:
  orchestrator:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
    env_file: ../../.env
    ports: ["8080:8080"]
    volumes:
      - ../../data:/app/data
    restart: unless-stopped
    mem_limit: 1g
    cpus: 2.0
