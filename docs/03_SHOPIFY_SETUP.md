# Configuration Shopify

Utiliser une application Shopify avec Admin GraphQL 2026-07. Configurer le domaine, le jeton Admin, le secret webhook, les scopes et une URL HTTPS publique. Exécuter ensuite `scripts/register_shopify_webhooks.py`.
