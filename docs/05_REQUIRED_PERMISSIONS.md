# Permissions

Les listes exécutables sont dans `config/shopify_scopes.yaml` et `config/alibaba_permissions.yaml`. Le mode réel doit rester désactivé tant que les probes ne confirment pas toutes les capacités critiques.
