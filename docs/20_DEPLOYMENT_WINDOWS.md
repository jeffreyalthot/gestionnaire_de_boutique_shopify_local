# Déploiement Windows

Utiliser les scripts PowerShell. Le planificateur de tâches démarre `run_forever.ps1` au démarrage et redémarre le runtime après une panne. Un service wrapper XML est également fourni.
