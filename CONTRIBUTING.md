# Contribution

Toute modification doit conserver l'idempotence, la reprise après panne, les
contrôles de permissions et la limite mémoire configurée. Exécuter
`python -m pytest` et `python -m compileall .` avant intégration.
