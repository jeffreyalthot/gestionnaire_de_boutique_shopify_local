mutation ProductSet($input: ProductSetInput!, $synchronous: Boolean!) { productSet(input: $input, synchronous: $synchronous) { product { id title status handle } productSetOperation { id status userErrors { field message } } userErrors { field message code } } }
